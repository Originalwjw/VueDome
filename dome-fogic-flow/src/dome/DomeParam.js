/**
 *
 *
 * @param {*} hash
 * @param {*} file
 * @param {*} chunkIndex
 * @param {*} totalchunks
 * @param {*} uploadedChunks
 * @param {*} chunksize
 */
//如何生成上述注释？
//光标在函数名上,快捷键 ctrl+alt+D
async function uploadchunk(
  hash,
  file,
  chunkIndex,
  totalchunks,
  uploadedChunks,
  chunksize
) {
  // 函数体
}
